var assert = require('assert');

describe("Operations on taxa", function() {
    
    describe("Adding a taxa", function() {

        it("should work if the taxa is unique");
        it("should not work if the taxa is not unique");
        it("should not work if the taxa has no name");

    });

});
